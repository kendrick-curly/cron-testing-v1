name: Google Chat Alerts

on:
  workflow_dispatch:

jobs:
  check-testing-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout testing branch
        uses: actions/checkout@v4
        with:
          ref: testing
          fetch-depth: 0   # needed for tag and history checks
          lfs: true


      - name: Check if commit has a tag
        id: tagcheck
        run: |
          if git describe --tags --exact-match >/dev/null 2>&1; then
            TAG=$(git describe --tags --exact-match)
            echo "Found tag: $TAG ${{github.ref_name}}"
          else
            echo "::error::❌ ${{github.ref_name}} This commit has NO TAG. Builds require a tag when using versioning: Tag."
            exit 1
          fi
       
      - name: testing
        id: sasd
        run: echo "test"
